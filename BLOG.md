# 给 A-SOUL 建一个追星站：从零到上线的全栈实践

> 零服务器成本，数据自动化更新，AI 帮我读日程表——这篇文章记录我为 A-SOUL 搭建动态聚合站的全过程，包括架构设计、核心功能实现，以及一路上踩过的那些坑。

## 前言

作为一个 A-SOUL 粉丝，日常追星有几个痛点：

1. **动态分散**：嘉然、贝拉、乃琳和官方四个账号，要在 B 站来回切换查看；
2. **直播预约容易漏**：直播预约动态淹没在信息流里，错过开播时间只能看回放；
3. **每周日程表是图片**：官方发的下周日程是一张图，时间安排全靠肉眼扫描，记不到日历里就没用。

市面上的三方工具要么年久失修，要么功能不合口味。索性自己动手，把"看动态、追直播"这件事做成一个自动化站点——这就是 [A-SOUL Timeline](https://asoul-timeline.pages.dev) 的由来。

## 整体架构

先上架构图：

```
本地 Mac（常驻任务，30 分钟/轮）         Cloudflare Free Plan
┌──────────────────────────┐           ┌────────────────────┐
│  数据采集服务              │           │  Workers (Hono)     │
│   ├─ 直播状态查询          │  增量推送  │   读 Turso          │
│   ├─ 动态获取（Playwright）│ ────────► │   /api/*            │
│   └─ AI 日程解析           │           └─────────┬──────────┘
└──────────────────────────┘                     │
                                      ┌──────────▼──────────┐
                                      │  Turso（边缘 SQLite）│
                                      └──────────▲──────────┘
                                                 │
                                      ┌──────────┴──────────┐
                                      │  Pages (Astro 静态)  │
                                      │  + SolidJS 岛屿      │
                                      └─────────────────────┘
```

设计思路可以概括成一句话：**本地负责"脏活"，云端负责"展示"**。

- B 站的接口对数据中心 IP 很敏感，所以数据获取放在本地 Mac 上跑（Playwright 驱动真实浏览器环境，后面细说）；
- 采集到的数据增量推送到 Turso——一个跑在边缘的 SQLite 服务；
- Cloudflare Workers 提供 API，读取 Turso 返回给前端；
- 前端是纯静态的 Astro + SolidJS 岛屿，构建产物直接部署到 Pages。

这样拆分的好处是：**免费额度完全够用**（Workers 每天 10 万次请求、Turso 每月 10 亿次读），而唯一需要"一直开机"的部分只是本地的一个定时任务。

## 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | Astro + SolidJS | 静态优先，交互部分用岛屿按需水合 |
| API | Hono | 轻量，同一套代码能同时跑在 Bun 和 Workers 上 |
| 数据库 | Turso + Drizzle ORM | 边缘 SQLite，HTTP 协议访问，免费额度慷慨 |
| 数据采集 | Bun + Playwright | 真实浏览器环境，稳定对接接口 |
| AI | 豆包 Seed 1.8 + DeepSeek | 多模态识图 + 文本分类，国内直连、价格友好 |
| Monorepo | Bun workspaces | 单仓管理 apps/packages，依赖安装快 |

其中有一个决策值得展开：**为什么 API 代码要写成"双模式"？**

```ts
// routes.ts 接收一个 db 实例，不关心底层是什么
export function createApp(db: any) { /* ... */ }

// 本地开发：bun:sqlite 直读文件
const db = createDb();

// 线上部署：libsql 读 Turso
const db = createTursoDb(env.TURSO_DB_URL, env.TURSO_AUTH_TOKEN);
```

同一份路由代码，本地跑 Bun、线上跑 Workers，靠启动入口注入不同的数据库客户端。开发环境和生产环境的行为完全一致，调试体验非常顺滑。

## 核心功能拆解

### 1. 动态时间线

时间线是最基础的功能，但"基础"不等于"简单"。B 站的动态结构相当复杂：

- **视频动态**：要提取封面和标题；
- **图文动态（Opus）**：新版图文的内容藏在 `major.opus` 里，图片在 `opus.pics`；
- **转发动态**：需要递归解析被转发的原动态，嵌套展示；
- **表情包**：文本里的 `[表情名]` 需要映射成真实的表情图片 URL；
- **直播预约卡片**：藏在 `additional.reserve` 里，自带标题和时间。

我的做法是在采集端就把这些结构"压平"成统一的富文本节点（`RichTextNode[]`）+ 结构化字段，存储时保留完整原始 JSON 以备后用，展示端只需渲染统一格式。

另外还有几个展示细节：

- B 站图片有防盗链，前端所有 `<img>` 必须加 `referrerpolicy="no-referrer"`，否则全是裂图；
- 图片展示用 `object-fit: contain` 而不是 `cover`，保证完整可见，点击放大查看原图；
- 每个成员有专属应援色（嘉然 `#E799B0`、贝拉 `#DB7D74`、乃琳 `#576690`、团体 `#FC966E`），通过 CSS 变量 `--theme-color` 注入卡片，边框、圆点、标签、ID 名全部跟随变化——单人动态用个人色，多人联动用团体色。

### 2. AI 识别每周日程表

这是整个项目里我最喜欢的功能。

官方每周发一张"日程表"图片，上面排布着一周的直播安排：日期、时间、标题、是单播/双播/团播、在谁的直播间。以前只能靠人肉看图记时间。

现在的流程是：

```
官方发布动态 → 初筛（文本含"日程表"关键词 + 带图）
            → 调用豆包 Seed 1.8 多模态接口读图
            → 模型返回结构化 JSON（日期/时间/标题/类型/参与成员）
            → 与动态正文交叉验证直播间归属（"X月X日…在 @XX 的直播间"）
            → 生成日程条目，写入日历
```

两个关键点：

**初筛省钱**。不是所有带图动态都要调多模态接口——先用关键词做一轮过滤，只有命中"日程表"的才触发识图，把 AI 调用成本压到最低。

**交叉验证防幻觉**。多模态模型偶尔会把直播间归属读错（比如团播的房主识别成"A-SOUL"而不是具体成员），所以解析结果会再和动态正文做正则匹配（`X月X日…@成员名…直播间`），用文字描述修正图片识别的偏差。

最终日程表动态的识别准确率相当高，参与成员会自动关联头像和全名，团播/双播的点击直达正确的直播间。

### 3. 直播日历

月视图日历，参考苹果日历的交互：

- 每天下方的应援色圆点表示当天有谁的直播；
- 点击某天展开当日时间线，条目按时间排列；
- 点击条目直接跳转 B 站直播间（而不是预约动态页）；
- 页面底部有"近期直播"汇总，按日期分组。

数据来源有两类，优先级不同：**AI 从日程表识别的条目**优先于**B 站预约卡片解析的条目**——官方日程表信息更全（含团播安排），同时间冲突时以它为准。

### 4. 正在直播

调 B 站的批量直播状态接口（`get_status_info_by_uids`），一次拿到三个人的开播状态、标题、观看人数。

这里有个架构上的坑：这个接口对 Cloudflare 数据中心 IP 风控很严，直接在 Workers 里调用会拿到空数据。所以同样走"本地查询 → 写库 → 云端读库"的链路，Workers 只从 Turso 读缓存结果。前端页面每 30 秒轮询一次，在播的成员卡片用应援色呼吸灯高亮。

### 5. 日历订阅

提供标准 ICS 文件（`/calendar/asoul_schedule.ics`），复制链接到苹果日历/Google 日历即可订阅。生成时注意了几个细节：

- `Content-Disposition: attachment` 强制下载而非浏览器内打开；
- 事件标题带参与成员前缀（`乃琳Queen - 【3D】无法通过的话`），在日历里一眼可辨；
- 每个事件的 `UID` 用预约 ID 保证幂等——重复订阅不会产生重复日程。

## 踩坑记录

### 坑一：接口风控与 -412

B 站对动态接口的保护相当严密。最初尝试纯 HTTP 方案：wbi 签名、buvid3/buvid4 cookie、_uuid、b_lsid、dm_img 设备指纹……全部补齐后依然返回 `-412`。原因在于这类检测不只看请求参数，还看 TLS 指纹和 JS 执行环境——这是任何手写 HTTP 客户端都很难完美模拟的。

**解法**：放弃手工模拟，改用 Playwright 驱动真实 Chromium。页面自然加载时，B 站自己的前端脚本会完成签名、cookie、指纹的全部工作，我只需要监听网络响应、拦截动态接口返回的 JSON。代码量骤减，稳定性反而大幅提升。

### 坑二：CDN 缓存导致的"灵异事件"

上线后陆续出现几个诡异现象：

- 自定义域名下点"加载更多"，反复出现同样的动态；
- `/api/live-status` 在这个域名有数据，换个域名就是空的；
- 明明更新了数据库，网页半天不刷新，过一阵又"自己好了"。

根源是同一个：**自定义域名开了 Cloudflare 代理（橙色云），CDN 把 API 的旧响应缓存了**。分页请求 `?cursor=xxx` 被缓存后，同一游标永远返回同一页。

**解法**：给所有 `/api/*` 响应统一加上：

```ts
Cache-Control: no-cache, no-store, must-revalidate
```

这类问题最隐蔽的地方在于"本地永远复现不了"——因为直连 `pages.dev` 不经过 CDN。遇到"换个域名表现不一样"的 bug，优先怀疑中间层缓存。

### 坑三：全量同步撑爆超时

数据量增长后，同步脚本开始报 `sync FAILED`。排查发现是每轮都把**全部**数据逐条推送到 Turso——138 条动态就是 138 次 HTTP 请求，轻松超过 60 秒超时。

**解法**：改成增量同步。本地用一个 `.last-sync` 文件记录上次同步时间戳，每轮只推 `fetched_at > lastSync` 的新数据：

| 场景 | 全量 | 增量 |
|---|---|---|
| 无新数据 | 138 行 / 60s+ | 0 行 / 4s |
| 有 2 条新动态 | 138 行 / 60s+ | 2 行 / 4s |

顺手加了网络重试（ECONNRESET 等瞬断自动重试 3 次），彻底告别偶发失败。

### 坑四：Mac 合盖就停摆

定时任务跑在本地 Mac 上，合盖休眠后一切暂停，唤醒后还留下一堆僵尸 Chromium 进程占着资源，导致后续任务连锁超时。

**解法**：三层防护——

1. `caffeinate -s` 阻止接电源状态下的系统睡眠；
2. 每轮任务开始前预清理残留的 Chromium 进程；
3. 子进程超时强杀 + 二次清理。

不过说到底，本地方案的宿命就是"电脑得开着"。长期方案已经想好：把这套常驻任务搬去便宜 VPS，配上住宅代理池，彻底解放本地机器。

## 部署与成本

整个站点的运行成本：

| 项目 | 费用 |
|---|---|
| 前端（Cloudflare Pages） | ¥0 |
| API（Cloudflare Workers） | ¥0 |
| 数据库（Turso Free） | ¥0 |
| AI 调用（豆包/DeepSeek，关键词初筛后调用量极低） | ≈¥0.5/月 |
| **合计** | **≈¥0.5/月** |

部署流程也极简：前端 `astro build` 后 `wrangler pages deploy`，API `wrangler deploy`，数据库 `turso db create` + 建表，全程不到十分钟。

## 项目结构

```
asoul_timeline/
├── apps/
│   ├── web/          # Astro 前端（Pages 部署）
│   └── api/          # Hono API（Workers 部署）
├── packages/
│   ├── crawler/      # 本地常驻数据服务
│   ├── db/           # Drizzle schema + 双模式客户端
│   └── shared/       # 共享类型定义
└── data/             # 本地 SQLite + 同步游标
```

几个值得借鉴的组织方式：

- **类型全链路共享**：`shared` 包定义 `Dynamic`、`LiveSchedule` 等接口，采集端写入、API 返回、前端渲染用的是同一套类型，改一处全局生效；
- **Schema 双端复用**：Drizzle 的 SQLite schema 同时服务于 `bun:sqlite` 和 libsql，本地和云端表结构永不漂移；
- **数据采集与展示彻底解耦**：常驻任务挂了只影响更新频率，站点本身照常服务——读写分离天然带来了容错。

## 结语

这个项目从立项到上线大概花了一周业余时间，最终形态是：打开网页就能看四个人最近的动态，直播安排自动进日历，开播了有高亮提醒，苹果日历还能订阅同步到手机。

技术上没什么惊世骇俗的东西，但"用自动化把重复劳动消灭掉"这件事本身乐趣十足——尤其是看到 AI 把那张日程表图片变成一条条结构化日程、准点出现在日历上的时候。

接下来的计划：把数据服务迁移到 VPS + 代理池，实现真正的 7×24 无人值守；日历订阅支持按成员过滤；时间线加上关键字订阅推送。

祝大家都能看到自己想看的直播！

*向枝江出发！*
